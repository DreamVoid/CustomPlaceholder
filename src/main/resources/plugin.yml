name: ${project.name}
author: DreamVoid
description: ${project.description}
main: me.dreamvoid.customplaceholder.main
version: ${project.version}
api-version: 1.13
prefix: DreamVoidPlugin
webside: ${project.url}
commands:
  customplaceholder:
    description: CustomPlaceholder 插件主命令
    permission: customplaceholder.command
    aliases:
      - cp
      - cplalceholder
permissions:
  customplaceholder.command:
    description: Allow use /customplaceholder
    default: op
depend:
  - PlaceholderAPI
